# ===================================================================
# Deep Agents Environment Configuration
# ===================================================================

# === Storage Backend (추상화 레이어) ===
# 옵션: local | s3
STORAGE_BACKEND=local

# S3 설정 (STORAGE_BACKEND=s3일 때)
S3_BUCKET_NAME=deep-agents-results
S3_REGION=us-east-1
S3_LIFECYCLE_DAYS=30

# Local 설정
LOCAL_DATA_DIR=./data

# === Graph Database Backend (추상화 레이어) ===
# 옵션: neo4j | neptune
GRAPH_DB_BACKEND=neo4j

# Neo4j Configuration
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=password

# Neptune 설정 (GRAPH_DB_BACKEND=neptune일 때)
# NEPTUNE_ENDPOINT=your-neptune-cluster.us-east-1.neptune.amazonaws.com
# NEPTUNE_PORT=8182
# NEPTUNE_USE_IAM=true

# === Vector Database Backend (추상화 레이어) ===
# 옵션: chromadb | pgvector
VECTOR_DB_BACKEND=chromadb

# ChromaDB Configuration
CHROMADB_HOST=localhost
CHROMADB_PORT=8000
CHROMADB_PERSIST_DIR=./data/chroma_db

# pgvector 설정 (VECTOR_DB_BACKEND=pgvector일 때)
# PGVECTOR_HOST=your-aurora-cluster.us-east-1.rds.amazonaws.com
# PGVECTOR_PORT=5432
# PGVECTOR_DATABASE=deep_agents
# PGVECTOR_USER=postgres
# PGVECTOR_PASSWORD=your-password

# === AWS Configuration ===
AWS_REGION=us-east-1
AWS_DEFAULT_REGION=us-east-1
AWS_BEDROCK_MODEL_ID_SONNET=anthropic.claude-3-5-sonnet-20241022-v2:0
AWS_BEDROCK_MODEL_ID_HAIKU=anthropic.claude-3-haiku-20240307-v1:0

# AWS Credentials (필요 시 - AWS Batch에서는 IAM Role 사용 권장)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key

# === Application Settings ===
# Tokenizers 설정 (fork 경고 방지)
TOKENIZERS_PARALLELISM=false

# Debug Logging Configuration
# 메인 에이전트 디버그 로깅 활성화 여부 (기본값: true)
ENABLE_DEBUG_LOGGING=true

# 서브 에이전트 디버그 로깅 활성화 여부 (기본값: false)
# 주의: 활성화 시 대량의 로그 파일 생성 (배치당 10개 코드 × N개 배치)
ENABLE_SUBAGENT_DEBUG_LOGGING=false

# ===================================================================
# AWS 프로덕션 환경 예시
# ===================================================================
# STORAGE_BACKEND=s3
# S3_BUCKET_NAME=deep-agents-results-prod
#
# GRAPH_DB_BACKEND=neo4j
# NEO4J_URI=bolt://your-neo4j-host:7687
# NEO4J_PASSWORD=${NEO4J_PASSWORD}  # Secrets Manager에서 주입
#
# VECTOR_DB_BACKEND=chromadb
# CHROMADB_PERSIST_DIR=/mnt/efs/chroma_db
# ===================================================================