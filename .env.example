# ===================================================================
# Deep Agents Environment Configuration
# ===================================================================

# === Storage Backend (추상화 레이어) ===
# 옵션: local | s3
STORAGE_BACKEND=local

# S3 설정 (STORAGE_BACKEND=s3일 때)
S3_BUCKET_NAME=deep-agents-results
S3_REGION=us-east-1
S3_LIFECYCLE_DAYS=30

# Local 설정
LOCAL_DATA_DIR=./data

# === Graph Database (Neo4j) ===
# Neo4j Configuration
# 로컬 개발: bolt://localhost:7687
# AWS EC2 (Docker): bolt://ec2-xxx-xxx-xxx-xxx.compute.amazonaws.com:7687
# t3.medium (4GB RAM) 권장 for MVP testing
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=password

# === Vector Database (ChromaDB) ===
# ChromaDB Configuration
# 로컬 개발: localhost:8000
# AWS EC2 (Docker): ec2-xxx-xxx-xxx-xxx.compute.amazonaws.com:8000
CHROMADB_HOST=localhost
CHROMADB_PORT=8000
CHROMADB_AUTH_TOKEN=  # 프로덕션에서는 필수 설정

# === AWS Configuration ===
AWS_REGION=us-east-1
AWS_DEFAULT_REGION=us-east-1
AWS_BEDROCK_MODEL_ID_SONNET=anthropic.claude-3-5-sonnet-20241022-v2:0
AWS_BEDROCK_MODEL_ID_HAIKU=anthropic.claude-3-haiku-20240307-v1:0

# AWS Credentials (필요 시 - AWS Batch에서는 IAM Role 사용 권장)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key

# === AWS Network Configuration (Optional) ===
# VPC 및 서브넷 자동 감지 실패 시 또는 특정 서브넷 사용 시 설정
# AWS_VPC_ID=vpc-xxxxxx
# AWS_SUBNET_IDS=subnet-aaaaaa,subnet-bbbbbb  # 쉼표로 구분, VPC Endpoint 지원 AZ에 있는 서브넷 권장

# === Application Settings ===
# Tokenizers 설정 (fork 경고 방지)
TOKENIZERS_PARALLELISM=false

# Debug Logging Configuration
# 메인 에이전트 디버그 로깅 활성화 여부 (기본값: true)
ENABLE_DEBUG_LOGGING=true

# 서브 에이전트 디버그 로깅 활성화 여부 (기본값: false)
# 주의: 활성화 시 대량의 로그 파일 생성 (배치당 10개 코드 × N개 배치)
ENABLE_SUBAGENT_DEBUG_LOGGING=false

# ===================================================================
# AWS EC2 프로덕션 환경 예시 (docker-compose.ec2.yml 사용)
# ===================================================================
# STORAGE_BACKEND=s3
# S3_BUCKET_NAME=deep-agents-results-prod
#
# NEO4J_URI=bolt://ec2-xxx.compute.amazonaws.com:7687
# NEO4J_USER=neo4j
# NEO4J_PASSWORD=${NEO4J_PASSWORD}  # docker-compose.ec2.yml에서 주입
#
# CHROMADB_HOST=ec2-xxx.compute.amazonaws.com
# CHROMADB_PORT=8000
# CHROMADB_AUTH_TOKEN=${CHROMA_AUTH_TOKEN}  # docker-compose.ec2.yml에서 주입
# ===================================================================